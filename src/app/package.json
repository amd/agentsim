{
  "name": "@agentsim/app",
  "version": "0.1.1",
  "private": true,
  "description": "The AgentSim Tauri desktop app: a TypeScript frontend (renderer) plus a Rust host in src-tauri.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "app:icons": "tauri icon assets/icon/dark.png"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1",
    "dompurify": "^3.4.11",
    "marked": "^18.0.5",
    "vis-timeline": "^7.7.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
